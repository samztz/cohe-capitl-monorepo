# Database Configuration
# This should match the PostgreSQL instance running via docker-compose
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/cohe_capital_dev

# Server Configuration
PORT=3001
HOST=0.0.0.0
NODE_ENV=development
LOG_LEVEL=info

# CORS Configuration
CORS_ORIGIN=*

# JWT Configuration
# IMPORTANT: Generate a secure random secret for production
# Example: openssl rand -base64 32
JWT_SECRET=your_secure_random_secret_here_min_32_chars

# Admin Authentication
# Token for admin panel access (development/demo only)
# In production, implement proper admin authentication with JWT
ADMIN_TOKEN=demo-admin-token

# SIWE (Sign-In with Ethereum) Configuration
# Domain where your app is hosted (no protocol, no port)
SIWE_DOMAIN=localhost
# Full URI of your application
SIWE_URI=http://localhost:3001
# Ethereum chain ID (1 = Mainnet, 56 = BSC, 5 = Goerli, 11155111 = Sepolia)
SIWE_CHAIN_ID=1

# BSC RPC (for Web3 payment verification)
RPC_BSC=https://bsc-dataseed.binance.org/
RPC_BSC_TESTNET=https://bsc-testnet.public.blastapi.io

# Treasury (optional - for future payment features)
TREASURY_ADDRESS=0xYourTreasuryWalletAddress

# Storage (optional - for future file upload features)
STORAGE_BUCKET_URL=https://your-bucket.r2.cloudflarestorage.com
STORAGE_ACCESS_KEY=your_access_key
STORAGE_SECRET_KEY=your_secret_key
