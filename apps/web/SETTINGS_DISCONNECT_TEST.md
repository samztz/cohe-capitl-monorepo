# Settings 页面 Disconnect Wallet 功能测试指南

## 功能概述

Settings 页面现在具有完整的钱包断开连接功能，包括：
- ✅ 真实钱包地址显示
- ✅ 确认对话框
- ✅ 加载状态
- ✅ 完整的清理流程

## 测试步骤

### 1. 准备工作
```bash
# 确保服务运行
# Web: http://localhost:3030
# API: http://localhost:3001
```

### 2. 登录流程
1. 访问 http://localhost:3030
2. 自动跳转到 `/auth/connect`
3. 点击 "Connect Wallet"
4. 选择钱包并完成 SIWE 签名
5. 成功登录后跳转到 Dashboard

### 3. 测试钱包地址显示
1. 导航到 Settings 页面
2. **验证 Header 右上角**显示钱包地址（格式：0xABCD...1234）
3. **验证 Account 部分**显示 "Wallet Address" 和完整地址
4. 确认地址与实际连接的钱包一致

### 4. 测试 Disconnect 按钮
1. 滚动到页面底部
2. 找到红色的 "Disconnect Wallet" 按钮
3. 点击按钮

### 5. 测试确认对话框
**应该看到**：
- ✅ 半透明黑色背景遮罩
- ✅ 居中的对话框卡片
- ✅ 红色警告图标
- ✅ "Disconnect Wallet?" 标题
- ✅ 提示文字："You will be logged out..."
- ✅ "Cancel" 和 "Disconnect" 两个按钮

**测试 Cancel**：
1. 点击 "Cancel" 按钮
2. 对话框应关闭
3. 保持在 Settings 页面，钱包仍然连接

### 6. 测试 Disconnect 流程
1. 再次点击 "Disconnect Wallet"
2. 在对话框中点击 "Disconnect"
3. **应该看到**：
   - ✅ 按钮显示 "Disconnecting..." 和 spinner 动画
   - ✅ 按钮变为 disabled 状态
4. **等待处理完成**：
   - ✅ Console 显示清理日志
   - ✅ 自动跳转到 `/auth/connect` 页面
   - ✅ 钱包连接已断开

### 7. 验证清理完成
打开浏览器 DevTools > Application > Local Storage：
- ✅ `auth_jwt_token` 已清除
- ✅ `auth_user_data` 已清除
- ✅ WalletConnect 相关 keys 已清除

### 8. 验证重新登录
1. 在 `/auth/connect` 页面
2. 点击 "Connect Wallet"
3. 能够正常重新连接和登录

## 预期行为

### 成功场景
✅ 钱包地址正确显示
✅ 确认对话框正常弹出和关闭
✅ Disconnect 按钮触发完整流程
✅ 加载状态正确显示
✅ 所有存储和会话完全清理
✅ 重定向到登录页面成功
✅ 可以重新登录

### Console 日志（正常情况）
```
[SettingsPage] Starting disconnect process...
[resetAuth] Starting full authentication reset...
[resetAuth] Disconnecting WalletConnect session...
[resetAuth] ✓ WalletConnect disconnected
[resetAuth] Clearing auth keys from localStorage...
[resetAuth] ✓ Auth keys cleared
[resetAuth] Scanning localStorage for WalletConnect/AppKit keys...
[resetAuth] Found X total keys in localStorage
[resetAuth] Removing Y WalletConnect/AppKit keys:
[resetAuth] ✓ WalletConnect/AppKit cache cleared
[resetAuth] ========================================
[resetAuth] Reset completed: SUCCESS
[resetAuth] Removed N storage keys
[resetAuth] ========================================
[AuthStore] Logged out successfully
[SettingsPage] Disconnect successful: {...}
```

## 故障排查

### 问题：点击 Disconnect 无反应
- 检查 Console 是否有错误
- 确认 `resetAuth` 函数正确导入
- 确认 `close` 函数从 AppKit 正确获取

### 问题：断开后仍然显示连接
- 检查 localStorage 是否真的清除
- 强制刷新页面（Ctrl+F5）
- 检查 authStore 状态是否清空

### 问题：无法重新登录
- 清除浏览器缓存和 cookies
- 关闭所有 MetaMask 弹窗
- 重启钱包扩展

## UI 预览

### Settings 页面
```
┌─────────────────────────────────────┐
│  [Logo] COHE.CAPITL    [0xAB...B064]│
├─────────────────────────────────────┤
│  Settings                           │
│  Manage your account and prefs      │
│                                     │
│  ACCOUNT                            │
│  ┌─────────────────────────────┐   │
│  │ 👤 Wallet Address           │   │
│  │    0xABCD...1234         →  │   │
│  └─────────────────────────────┘   │
│                                     │
│  ...                                │
│                                     │
│  ┌─────────────────────────────┐   │
│  │ 🚪 Disconnect Wallet        │   │ ← 红色按钮
│  └─────────────────────────────┘   │
└─────────────────────────────────────┘
```

### 确认对话框
```
┌─────────────────────────────────────┐
│                                     │
│          ⚠️ (红色圆圈)              │
│                                     │
│      Disconnect Wallet?             │
│                                     │
│  You will be logged out and need   │
│  to reconnect your wallet...       │
│                                     │
│  ┌──────────┐  ┌──────────────┐    │
│  │  Cancel  │  │  Disconnect  │    │ ← 红色
│  └──────────┘  └──────────────┘    │
└─────────────────────────────────────┘
```

## 完成 ✅

所有功能已实现并可以测试！
